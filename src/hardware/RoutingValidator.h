#pragma once

#include <HardwareDrivers/RoutingValidator.h>

namespace abdaudiolab::hardware
{
    using SubmoduleDescriptor = abd::hw::SubmoduleDescriptor;
    using RoutingValidator = abd::hw::RoutingValidator;
}
